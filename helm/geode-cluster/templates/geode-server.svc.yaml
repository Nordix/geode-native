apiVersion: v1
kind: Service
metadata:
  name: {{ include "geode-cluster.server.name" . }}
  labels:
    app: {{ include "geode-cluster.server.name" . }}
spec:
  ports:
  - port: {{ .Values.cluster.server.memberPort }}
  clusterIP: None
  selector:
    app: {{ include "geode-cluster.server.name" . }}
